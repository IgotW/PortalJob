@model IEnumerable<JobSystem.Entities.JobPosting>

@{
	ViewData["Title"] = "CandidateDashPage";
}

@* <table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CompanyName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Salary)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PostedDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CompanyName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Salary)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PostedDate)
            </td>
            <td>
                <a asp-action="Apply" asp-route-id="@item.Id">Apply</a> |
                <a asp-action="DetailsCan" asp-route-id="@item.Id">Details</a>
            </td>
        </tr>
}
    </tbody>
</table> *@

	@* <div class="container-fluid py-4">
	<div class="row">
	<!-- Left Sidebar - Filters -->
	<div class="col-md-3">
	<div class="card shadow-sm mb-4">
	<div class="card-body">
	<h5 class="mb-3">Filters</h5>
	<form>
	<!-- Job Type Filters -->
	<div class="mb-3">
	<label class="form-label">Job Type</label>

	<div>
	<div class="form-check">
	<input type="radio" name="searchBy" value="All" id="all" class="form-check-input" checked />
	<label for="all" class="form-check-label">All</label>
	</div>
	<div class="form-check">
	<input type="radio" name="searchBy" value="Full-Time" id="fullTime" class="form-check-input" />
	<label for="fullTime" class="form-check-label">Full-Time</label>
	</div>
	<div class="form-check">
	<input type="radio" name="searchBy" value="Part-Time" id="partTime" class="form-check-input" />
	<label for="partTime" class="form-check-label">Part-Time</label>
	</div>
	<div class="form-check">
	<input type="radio" name="searchBy" value="Remote" id="remote" class="form-check-input" />
	<label for="remote" class="form-check-label">Remote</label>
	</div>
	</div>

	</div>
	</form>
	</div>
	</div>
	</div> *@

	<!-- Main Content -->
	<div class="container">
		<div class="row justify-content-center">
			<div class="col-md-9">
				<!-- Search Bar -->
				<div class="card shadow-sm mb-4">
					<div class="card-body">
						<h5 class="mb-3 text-center">Search Jobs</h5> <!-- Center the heading -->
						<form class="d-flex gap-2 justify-content-center" asp-action="CandidateDashPage" asp-controller="Account">
							<input type="search" name="searchString" class="form-control" placeholder="Search jobs..." style="max-width: 400px;"> <!-- Limit input width -->
							<button type="submit" class="btn btn-primary">
								<i class="bi bi-search"></i> Search
							</button>
						</form>
					</div>
				</div>
			</div>
		</div>
	</div>

	@*     @using (Html.BeginForm("CandidateDashPage", "Account", FormMethod.Get))
	{
	<b>Search by: </b>
	@Html.RadioButton("SearchBy", "All", true) <text>All</text>
	@Html.RadioButton("SearchBy", "Full-Time", true) <text>Full-Time</text>
	@Html.RadioButton("SearchBy", "Part-Time", true) <text>Part-Time</text>
	@Html.RadioButton("SearchBy", "Remote", true) <text>Remote</text>
	<input type="submit" value="Apply" class="btn btn-success" />
	<div class="col-md-9">
	<div class="card shadow-sm mb-4">
	<div class="card-body">
	<h5 class="mb-3">Search Jobs</h5>
	<form class="d-flex gap-2">
	<input type="search" class="form-control" placeholder="Search jobs...">
	<button type="submit" class="btn btn-primary">
	<i class="bi bi-search"></i> Search
	</button>
	</form>
	</div>
	</div>
	</div>
	}
	</p>

	@if (Model.Count() == 0)
	{
	<tr>
	<td colspan="3" class="text-danger fw-bold">No data found</td>
	</tr>
	}
	@* <div class="container-fluid py-4">
	<div class="row">
	<!-- Left Sidebar - Filters -->
	<div class="col-md-3">
	<div class="card shadow-sm mb-4">
	<div class="card-body">
	<h5 class="mb-3">Filters</h5>
	<form>
	<!-- Job Type Filters -->
	<div class="mb-3">
	<label class="form-label">Job Type</label>
	<div>
	<div class="form-check">
	<input class="form-check-input" type="checkbox" id="fullTime">
	<label class="form-check-label" for="fullTime">Full-time</label>
	</div>
	<div class="form-check">
	<input class="form-check-input" type="checkbox" id="partTime">
	<label class="form-check-label" for="partTime">Part-time</label>
	</div>
	<div class="form-check">
	<input class="form-check-input" type="checkbox" id="remote">
	<label class="form-check-label" for="remote">Remote</label>
	</div>
	</div>
	</div>
	<button type="submit" class="btn btn-primary w-100">Apply Filters</button>
	</form>
	</div>
	</div>
	</div>

	<!-- Main Content -->
	<div class="col-md-9">
	<!-- Search Bar -->
	<div class="card shadow-sm mb-4">
	<div class="card-body">
	<h5 class="mb-3">Search Jobs</h5>
	<form class="d-flex gap-2">
	<input type="search" class="form-control" placeholder="Search jobs...">
	<button type="submit" class="btn btn-primary">
	<i class="bi bi-search"></i> Search
	</button>
	</form>
	</div>
	</div> *@

	<!-- Job Listings -->
	<div class="card shadow-sm">
		<div class="card-body">
			<h5 class="mb-3">Job Listings</h5>
			<!-- Dynamic job listings will go here -->
		@if (!Model.Any())
		{
			<div class="text-danger fw-bold">No data found</div>
		}
		else
		{
			<div class="row g-4">
				@foreach (var job in Model)
				{
					<div class="col-12">
						<div class="card shadow-sm">
							<div class="card-body">
								<div class="d-flex justify-content-between align-items-center">
									<div>
										<h5 class="card-title">@job.Title</h5>
										<h6 class="card-subtitle mb-2 text-muted">
											@job.CompanyName • @job.Location
										</h6>
									</div>
									<div class="btn-group">
										<a asp-action="Apply" asp-route-id="@job.Id" class="btn btn-sm btn-outline-primary">
											<i class="bi bi-pencil"></i> Apply
										</a>
										<a asp-action="DetailsCan" asp-route-id="@job.Id" class="btn btn-sm btn-outline-info">
											<i class="bi bi-eye"></i> Details
										</a>
									</div>
								</div>

								<div class="mb-2">
									<span class="badge bg-success me-2">@job.Type</span>
									<span class="badge bg-info">@job.Salary</span>
								</div>

								<p class="card-text">@job.Description</p>
								<small class="text-muted">Posted @job.PostedDate.ToString("MMM dd, yyyy")</small>
							</div>
						</div>
					</div>
				}
			</div>
		}
	</div>
</div>



